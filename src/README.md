# src/

Codigo da aplicacao. Vazio ate a change `fundacao-aplicacao` criar a
solution, o `SharedKernel`, o `Platform`, o host `Api` e os modulos em
`Modules/`.

Ver `openspec/changes/fundacao-aplicacao/` para o planejamento.
