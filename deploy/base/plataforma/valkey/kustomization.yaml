# Onda -2 (D3 do design): Postgres, Valkey e MinIO sobem juntos, depois
# dos operadores/controllers (onda -4) e dos segredos (onda -3).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: data
resources:
  - deployment.yaml
  - service.yaml
commonAnnotations:
  argocd.argoproj.io/sync-wave: "-2"
